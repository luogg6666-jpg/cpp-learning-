# 昨天学习了cpp在visual studio的运行

## 关于运行代码全过程的我的理解

一开始是自己写的那些代码先被预处理（例如将头文件转换为原来的样子，生成为纯代码文件），然后再将预处理完的代码转化为汇编语言（人可以看懂的写给cpu的语言，但cpu还是看不懂），接下来继续将汇编语言转换为机器语言（类似于010101001这样的二进制语言），并通过obj格式的文件来储存，这些obj文件会通过链接器被组合为一个exe格式的文件，此文件可以直接打开，并直接在终端输出结果
