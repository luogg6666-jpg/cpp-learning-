# 今天学了两个对立的函数，构造函数和析构函数
# 第一个函数有助于简化对象的构建
正常如下
```cpp
    Dog d;
    d.name = "旺财";
    d.age = 3;
```
使用构造函数
```cpp
    Dog d("旺财",3);
```
可以看到简化了很多
# 析构函数相反，是用于把创造的对象删除，使用方法就是在函数名前面加个~行就行,使用析构函数是为了防止内存的泄露，因为一开始的构造函数是在堆上面申请的内存
# 正常情况下构造函数和析构函数都会自动使用，不需要额外打
